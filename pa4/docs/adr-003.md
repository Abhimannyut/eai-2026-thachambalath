# ADR-003 — <the decision, as a short noun phrase>

<!--
PA4 architecture decision record. Required: a public test checks that the
four `##` headings below are present, and the manual 30% of this assignment
is the quality of what you write under them.

Keep the headings exactly as they are. One page is enough.

The question this ADR is really asking you:

    You mapped three unrelated formats onto a canonical model you do not own,
    enriched them from a service that can fail, and removed data that must
    not travel. Where, specifically, did each of those three things bite you
    - and what would you build differently if this canonical model had to
    outlive this course?

Write it after you have finished the code, while the annoyance is fresh.
-->

**Status:** accepted
**Date:** YYYY-MM-DD

## Context

<!--
What did the three sources actually do to you? Be specific and concrete -
name the field, the format, the HTTP status. "The XML was annoying" is not
context; "the file declared encoding="windows-1257" and reading it as UTF-8
turned Bērziņa into B?rzi?a without an error, and the same bytes read as
latin1 produced a different but equally wrong string" is.

Worth naming if you hit them: the CP1257-declared XML, fast-xml-parser's
attribute prefix and its object-vs-array behaviour for a single <LineItem>,
the mobile epoch, the three different product-id formats, an unknown
product id, a pricing API that returns 401/404/500, and where each source
hid its payment details.
-->

## Decision

<!--
What you built, specifically enough that I could find it in your code. Name
the function and what it does. Include the decisions that are easy to skip
over:

- Why unitPrice is a decimal string, and exactly how you turned the pricing
  API's number into one (what happened to 4.5?).
- How you represent "this product id came back 404" or "the pricing API
  returned 500" in your TransformResult, and what you did with the item in
  that case - dropped it? left the order out entirely? Say which, and why.
- Where each source's payment data lived, and what you did with it (not
  copying a field is still a decision worth stating explicitly).
-->

## Alternatives considered

<!--
At least one real alternative with the reason you rejected it. Real options
here include: storing unitPrice as an integer number of cents instead of a
decimal string; failing the whole order when one line item cannot be priced,
instead of dropping just that item; caching pricing responses instead of
calling the API on every item; validating against the schema yourself inside
the translator instead of leaving that to the tests; hand-rolling an XML
parser instead of using fast-xml-parser; treating an unrecognized currency
or status code as an error instead of passing it through.
-->

## Consequences

<!--
What is now easier, what is now harder, and what breaks first.

Concretely: what happens to your code when the canonical schema (which you
do not own) gains a required field next semester? When the pricing API adds
a product your normalization table has never seen? When a fourth source
arrives with a product id format none of your three normalization branches
recognize? Name a cost you accepted - a consequences section with no
downside in it is not credible.
-->
